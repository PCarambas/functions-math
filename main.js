// Write a function to add two values
// Add the values of the two parameters and return the results

const addValues = (num1, num2) => {
    return num1 + num2
}





// Write a function to multiply a single number by 2
// Multiply the value in the parameter by 2 and return the result.

const multiply = (number) => {
    return  number * 2
}

const sum = addValues(4, 14)
const total = multiply(sum)

console.log(total)